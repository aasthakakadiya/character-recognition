# character-recognition
Character Recognition using ANN and CNN is an AI-based project that identifies handwritten characters from images. CNN is used to extract visual features, while ANN performs classification. The system is trained on labeled data and applies basic preprocessing for accurate recognition.
